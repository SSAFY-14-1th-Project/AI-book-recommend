# 카카오톡 오픈채팅 URL 연동 구현 가이드

## 핵심 문제점

카카오톡은 **"버튼 클릭 → 자동으로 오픈채팅 URL 가져오기" 기능을 API로 제공하지 않습니다.**

### 카카오 API의 한계
- 카카오 로그인 API는 사용자 프로필 정보(이름, 이메일 등)만 제공
- **오픈채팅방 URL을 프로그래밍적으로 생성하거나 조회하는 API가 없음**
- 오픈채팅은 카카오톡 앱 내에서만 생성 가능

---

## 현실적인 구현 방법

### 사용자가 직접 URL 입력 (가장 일반적)

당근마켓, 번개장터 등 대부분의 서비스가 이 방식을 사용합니다.

**흐름:**
1. 사용자가 카카오톡 앱에서 오픈채팅방 생성
2. 오픈채팅 프로필 링크 복사 (`https://open.kakao.com/o/xxxxx`)
3. 우리 사이트 게시글 작성 시 해당 URL 붙여넣기
4. 백엔드에서 URL 형식 검증 (`https://open.kakao.com/` 으로 시작하는지)

---

## 변경 이력

### 2024-12-25 업데이트

#### 발견된 문제점 및 해결

1. **문제: DB에 `kakao_chat_url` 값이 저장되지 않음**
   - 원인: `TradeSerializer`의 `fields`에 `kakao_chat_url`이 없었음
   - 해결: `fields`에 추가 + `write_only=True` 설정으로 목록 응답에서는 숨김

2. **문제: 프론트엔드 페이지가 빈 화면**
   - 원인: `TradeListView.vue` 파일이 비어있었고, `TradeCreateView.vue`에 필수 변수/함수 누락
   - 해결: 두 파일 모두 완전히 재작성

---

## Backend 구현 (완료)

### `backend/trades/serializers.py`

```python
import re

class TradeSerializer(serializers.ModelSerializer):
    """게시글 등록 및 목록 조회용"""
    user = UserSimpleSerializer(read_only=True)
    book = BookTradeSerializer(read_only=True)

    # 핵심: write_only=True로 설정하면
    # - POST 요청 시: 값을 받아서 DB에 저장 ✅
    # - GET 응답 시: 응답에 포함되지 않음 ✅ (목록에서 URL 숨김)
    kakao_chat_url = serializers.CharField(write_only=True, required=False, allow_blank=True)

    def validate_kakao_chat_url(self, value):
        """카카오톡 오픈채팅 URL 형식 검증"""
        if value:
            pattern = r'^https://open\.kakao\.com/o/[a-zA-Z0-9]+$'
            if not re.match(pattern, value):
                raise serializers.ValidationError(
                    "올바른 카카오톡 오픈채팅 URL 형식이 아닙니다."
                )
        return value

    class Meta:
        model = Trade
        fields = [
            'id', 'user', 'book', 'title', 'content',
            'sale_type', 'price', 'region', 'status',
            'image', 'view_count', 'created_at', 'kakao_chat_url'
        ]
        read_only_fields = ['view_count', 'created_at', 'updated_at']


class TradeDetailSerializer(serializers.ModelSerializer):
    """게시글 단일 조회(상세 페이지)용"""
    user = UserSimpleSerializer(read_only=True)
    book = BookTradeSerializer(read_only=True)

    def to_representation(self, instance):
        """응답 데이터 커스터마이징 - 본인 게시글이면 카카오 URL 숨기기"""
        data = super().to_representation(instance)
        request = self.context.get('request')

        # 본인 게시글이면 kakao_chat_url 제거
        # 다른 사용자나 비로그인 사용자에게는 URL이 보임
        if request and request.user.is_authenticated:
            if request.user == instance.user:
                data.pop('kakao_chat_url', None)

        return data

    class Meta:
        model = Trade
        fields = [
            'id', 'user', 'book', 'title', 'content',
            'sale_type', 'price', 'region', 'status',
            'image', 'kakao_chat_url', 'view_count',
            'created_at', 'updated_at'
        ]
        read_only_fields = ['view_count', 'created_at', 'updated_at']
```

---

## Frontend 구현 (완료)

### 1. API 파일 - `frontend/src/api/trades.js`

```javascript
import client from './client'

// 중고거래 목록 조회
export const getTradeList = () => {
  return client.get('/api/trades/')
}

// 중고거래 상세 조회
export const getTradeDetail = (tradeId) => {
  return client.get(`/api/trades/${tradeId}/`)
}

// 중고거래 생성
export const createTrade = (bookPk, tradeData) => {
  return client.post(`/api/trades/create/${bookPk}/`, tradeData)
}

// 중고거래 수정
export const updateTrade = (tradeId, tradeData) => {
  return client.put(`/api/trades/${tradeId}/`, tradeData)
}

// 중고거래 삭제
export const deleteTrade = (tradeId) => {
  return client.delete(`/api/trades/${tradeId}/`)
}

// 중고거래 검색
export const searchTrades = (params) => {
  return client.get('/api/trades/search/', { params })
}
```

---

### 2. 라우터 - `frontend/src/router/index.js`

```javascript
// children 배열에 추가
{
  path: 'trades',
  name: 'trade-list',
  component: () => import('@/views/TradeListView.vue'),
},
{
  path: 'trades/create',
  name: 'trade-create',
  component: () => import('@/views/TradeCreateView.vue'),
  meta: { requiresAuth: true }
},
{
  path: 'trades/:id',
  name: 'trade-detail',
  component: () => import('@/views/TradeDetailView.vue'),
},
```

---

### 3. 게시글 목록 - `frontend/src/views/TradeListView.vue`

```vue
<template>
  <div class="trade-list container mx-auto p-4">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-2xl font-bold">중고거래</h1>
      <router-link :to="{ name: 'trade-create' }" class="btn btn-primary">
        글쓰기
      </router-link>
    </div>

    <!-- 거래 목록 -->
    <div v-if="trades.length > 0" class="grid gap-4">
      <div
        v-for="trade in trades"
        :key="trade.id"
        class="card card-side bg-base-100 shadow-md cursor-pointer hover:shadow-lg transition-shadow"
        @click="goToDetail(trade.id)"
      >
        <figure class="w-32 h-32 flex-shrink-0">
          <img
            :src="trade.image || '/placeholder.png'"
            :alt="trade.title"
            class="w-full h-full object-cover"
          />
        </figure>
        <div class="card-body p-4">
          <h2 class="card-title text-lg">{{ trade.title }}</h2>
          <p class="text-sm text-gray-500">{{ trade.region }} · {{ formatDate(trade.createdAt) }}</p>
          <p class="font-bold text-lg">
            {{ trade.saleType === 'free' ? '무료나눔' : `${trade.price?.toLocaleString()}원` }}
          </p>
          <div class="flex gap-2">
            <span class="badge" :class="getStatusBadgeClass(trade.status)">
              {{ getStatusText(trade.status) }}
            </span>
          </div>
        </div>
      </div>
    </div>

    <div v-else class="text-center py-12 text-gray-500">
      <p>등록된 거래가 없습니다.</p>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { getTradeList } from '@/api/trades'

const router = useRouter()
const trades = ref([])

onMounted(async () => {
  try {
    const response = await getTradeList()
    trades.value = response.data
  } catch (error) {
    console.error('거래 목록 조회 실패:', error)
  }
})

const goToDetail = (id) => {
  router.push({ name: 'trade-detail', params: { id } })
}

const formatDate = (dateString) => {
  if (!dateString) return ''
  const date = new Date(dateString)
  const now = new Date()
  const diff = now - date
  const days = Math.floor(diff / (1000 * 60 * 60 * 24))

  if (days === 0) return '오늘'
  if (days === 1) return '어제'
  if (days < 7) return `${days}일 전`
  return date.toLocaleDateString('ko-KR')
}

const getStatusText = (status) => {
  const statusMap = {
    available: '판매중',
    reserved: '예약중',
    sold: '판매완료'
  }
  return statusMap[status] || status
}

const getStatusBadgeClass = (status) => {
  const classMap = {
    available: 'badge-success',
    reserved: 'badge-warning',
    sold: 'badge-ghost'
  }
  return classMap[status] || ''
}
</script>
```

---

### 4. 게시글 생성 - `frontend/src/views/TradeCreateView.vue`

**주요 기능:**
- 도서 자동완성 검색 (2글자 이상 입력 시)
- 도서 선택/취소
- 이미지 업로드 및 미리보기
- 카카오톡 오픈채팅 URL 입력
- 지역 선택 (models.py의 REGION_CHOICES와 동일)

```vue
<template>
  <div class="trade-create container mx-auto p-4 max-w-2xl">
    <h1 class="text-2xl font-bold mb-6">중고거래 등록</h1>

    <form @submit.prevent="handleSubmit" class="space-y-4">
      <!-- 도서 선택 -->
      <div class="form-control">
        <label class="label">
          <span class="label-text">도서 선택 *</span>
        </label>
        <div class="relative">
          <input
            v-model="bookSearch"
            @input="searchBooks"
            @focus="showDropdown = true"
            type="text"
            placeholder="도서명을 검색하세요..."
            class="input input-bordered w-full"
          />
          <!-- 자동완성 드롭다운 -->
          <ul
            v-if="showDropdown && bookResults.length > 0"
            class="absolute z-10 w-full bg-base-100 border rounded-lg mt-1 max-h-60 overflow-y-auto shadow-lg"
          >
            <li
              v-for="book in bookResults"
              :key="book.id"
              @click="selectBook(book)"
              class="p-3 hover:bg-base-200 cursor-pointer"
            >
              <p class="font-medium">{{ book.title }}</p>
              <p class="text-sm text-gray-500">{{ book.author }}</p>
            </li>
          </ul>
        </div>
        <!-- 선택된 도서 표시 -->
        <div v-if="selectedBook" class="mt-2 p-3 bg-base-200 rounded-lg flex justify-between">
          <div>
            <p class="font-medium">{{ selectedBook.title }}</p>
            <p class="text-sm text-gray-500">{{ selectedBook.author }}</p>
          </div>
          <button type="button" @click="clearBook" class="btn btn-ghost btn-sm">✕</button>
        </div>
      </div>

      <!-- 카카오톡 오픈채팅 URL -->
      <div class="form-control">
        <label class="label">
          <span class="label-text">카카오톡 오픈채팅 링크</span>
        </label>
        <input
          v-model="form.kakaoChatUrl"
          type="url"
          placeholder="https://open.kakao.com/o/..."
          class="input input-bordered w-full"
        />
        <label class="label">
          <span class="label-text-alt text-gray-500">
            카카오톡 → 채팅 → + → 오픈채팅 → 1:1 채팅 → 프로필 설정 → 링크 복사
          </span>
        </label>
      </div>

      <!-- ... 기타 필드 (제목, 내용, 가격, 지역, 이미지 등) ... -->
    </form>
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue'
import { useRouter } from 'vue-router'
import { createTrade } from '@/api/trades'
import client from '@/api/client'

const router = useRouter()

const form = reactive({
  title: '',
  content: '',
  saleType: 'sale',
  price: 0,
  region: 'seoul',
  kakaoChatUrl: '',
  image: null
})

const bookSearch = ref('')
const bookResults = ref([])
const selectedBook = ref(null)
const showDropdown = ref(false)
const imagePreview = ref(null)

// 도서 검색 (디바운스 적용)
let searchTimeout = null
const searchBooks = () => {
  clearTimeout(searchTimeout)
  searchTimeout = setTimeout(async () => {
    if (bookSearch.value.length < 2) {
      bookResults.value = []
      return
    }
    try {
      const response = await client.get('/api/books/autocomplete/', {
        params: { q: bookSearch.value }
      })
      bookResults.value = response.data
    } catch (error) {
      console.error('도서 검색 실패:', error)
    }
  }, 300)
}

const selectBook = (book) => {
  selectedBook.value = book
  bookSearch.value = ''
  bookResults.value = []
  showDropdown.value = false
}

const clearBook = () => {
  selectedBook.value = null
}

const handleImageUpload = (event) => {
  const file = event.target.files[0]
  if (file) {
    form.image = file
    const reader = new FileReader()
    reader.onload = (e) => {
      imagePreview.value = e.target.result
    }
    reader.readAsDataURL(file)
  }
}

const handleSubmit = async () => {
  if (!selectedBook.value) {
    alert('도서를 선택해주세요.')
    return
  }

  try {
    const formData = new FormData()
    formData.append('title', form.title)
    formData.append('content', form.content)
    formData.append('sale_type', form.saleType)
    formData.append('price', form.saleType === 'free' ? 0 : form.price)
    formData.append('region', form.region)
    if (form.kakaoChatUrl) {
      formData.append('kakao_chat_url', form.kakaoChatUrl)
    }
    if (form.image) {
      formData.append('image', form.image)
    }

    await createTrade(selectedBook.value.id, formData)
    alert('게시글이 등록되었습니다.')
    router.push({ name: 'trade-list' })
  } catch (error) {
    console.error('게시글 등록 실패:', error)
  }
}
</script>
```

---

### 5. 게시글 상세 - `frontend/src/views/TradeDetailView.vue`

```vue
<template>
  <div class="trade-detail" v-if="trade">
    <!-- 상품/판매자 정보 -->

    <!-- 채팅하기 버튼 -->
    <!-- 백엔드에서 본인 게시글이면 kakaoChatUrl이 null로 옴 -->
    <a
      v-if="trade.kakaoChatUrl"
      :href="trade.kakaoChatUrl"
      target="_blank"
      class="btn btn-primary chat-btn"
    >
      💬 채팅하기
    </a>
  </div>
</template>
```

---

## 테스트 방법

### 1. 서버 실행

```bash
# 터미널 1: Django 백엔드
cd backend
python manage.py runserver
# http://localhost:8000

# 터미널 2: Vue 프론트엔드
cd frontend
npm run dev
# http://localhost:5173
```

### 2. 테스트 URL

| 용도 | URL |
|------|-----|
| Swagger UI | http://localhost:8000/api/schema/swagger-ui/ |
| 프론트엔드 | http://localhost:5173 |
| 거래 목록 | http://localhost:5173/trades |
| 거래 등록 | http://localhost:5173/trades/create |
| 거래 상세 | http://localhost:5173/trades/{id} |

### 3. Postman 테스트

**게시글 생성 테스트:**
```
POST http://localhost:8000/api/trades/create/{book_pk}/
Authorization: Bearer {token}
Content-Type: multipart/form-data

Body:
- title: 테스트 제목
- content: 테스트 내용
- sale_type: sale
- price: 10000
- region: seoul
- kakao_chat_url: https://open.kakao.com/o/abc123
```

**본인 게시글 URL 숨김 테스트:**
```
# 작성자로 조회 → kakao_chat_url 없음
GET http://localhost:8000/api/trades/1/
Authorization: Bearer {작성자토큰}

# 다른 사용자로 조회 → kakao_chat_url 있음
GET http://localhost:8000/api/trades/1/
Authorization: Bearer {다른사용자토큰}

# 비로그인으로 조회 → kakao_chat_url 있음
GET http://localhost:8000/api/trades/1/
(Authorization 헤더 없음)
```

---

## 요약 체크리스트

| 위치 | 파일 | 작업 | 상태 |
|------|------|------|------|
| **Backend** | `trades/serializers.py` | `kakao_chat_url` write_only 설정 + URL 검증 + 본인 숨기기 | ✅ 완료 |
| **Frontend** | `api/trades.js` | API 함수들 | ✅ 완료 |
| **Frontend** | `router/index.js` | 라우트 추가 | ✅ 완료 |
| **Frontend** | `views/TradeListView.vue` | 거래 목록 페이지 | ✅ 완료 |
| **Frontend** | `views/TradeCreateView.vue` | 거래 등록 페이지 (도서 검색, 이미지 업로드, 카카오 URL) | ✅ 완료 |
| **Frontend** | `views/TradeDetailView.vue` | 거래 상세 페이지 (채팅하기 버튼) | ✅ 완료 |

---

## 참고: 오픈채팅 링크 만드는 방법 (사용자 가이드용)

1. 카카오톡 앱 실행
2. 하단 "채팅" 탭 선택
3. 우측 상단 "+" 버튼 클릭
4. "오픈채팅" 선택
5. "1:1 채팅" 선택
6. 오픈 프로필 설정 (닉네임, 프로필 사진)
7. "링크 복사" 클릭
8. 복사된 링크를 게시글에 붙여넣기
